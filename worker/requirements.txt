# Base worker deps — the light, GPU-free, AWS-free set installed in CI.
# The heavy GPU stack (torch / nnsight / vllm) lives in requirements-gpu.txt and is
# imported lazily only inside the real engine paths, so FORAY_FAKE=1 and the unit
# tests run with just these. Copyright 2026 Scott Friedman. Apache License 2.0.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
boto3>=1.35
# test + lint
pytest>=8.3
httpx>=0.27          # FastAPI TestClient transport
ruff>=0.7
